export const environment = {
  production: true,
  apiUrl: 'https://${API_GATEWAY_ID}.execute-api.${AWS_REGION}.amazonaws.com/prod',
  authUrl: 'https://${API_GATEWAY_ID}.execute-api.${AWS_REGION}.amazonaws.com/prod/auth'
};